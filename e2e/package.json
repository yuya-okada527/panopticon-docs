{
  "name": "e2e",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "@playwright/test": "^1.18.1"
  },
  "scripts": {
    "start": "playwright test",
    "open": "playwright show-report",
    "setup": "playwright install chromium",
    "install": "playwright install chromium",
    "codegen": "playwright codegen localhost:4000"
  }
}
